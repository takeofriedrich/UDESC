import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:app_mobile/app/modules/home/comprar/selecionar_origem/selecionar_origem_page.dart';

void main() {
  testWidgets('SelecionarOrigemPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SelecionarOrigemPage(title: 'SelecionarOrigem')));
    //  final titleFinder = find.text('SelecionarOrigem');
    //  expect(titleFinder, findsOneWidget);
  });
}
