import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:app_mobile/app/modules/home/comprar/selecionar_origem/selecionar_origem_controller.dart';
import 'package:app_mobile/app/modules/home/comprar/comprar_module.dart';

void main() {
  initModule(ComprarModule());
  // SelecionarOrigemController selecionarorigem;
  //
  setUp(() {
    //     selecionarorigem = ComprarModule.to.get<SelecionarOrigemController>();
  });

  group('SelecionarOrigemController Test', () {
    //   test("First Test", () {
    //     expect(selecionarorigem, isInstanceOf<SelecionarOrigemController>());
    //   });

    //   test("Set Value", () {
    //     expect(selecionarorigem.value, equals(0));
    //     selecionarorigem.increment();
    //     expect(selecionarorigem.value, equals(1));
    //   });
  });
}
