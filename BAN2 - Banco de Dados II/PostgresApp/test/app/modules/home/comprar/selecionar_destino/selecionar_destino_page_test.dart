import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:app_mobile/app/modules/home/comprar/selecionar_destino/selecionar_destino_page.dart';

void main() {
  testWidgets('SelecionarDestinoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SelecionarDestinoPage(title: 'SelecionarDestino')));
    //  final titleFinder = find.text('SelecionarDestino');
    //  expect(titleFinder, findsOneWidget);
  });
}
