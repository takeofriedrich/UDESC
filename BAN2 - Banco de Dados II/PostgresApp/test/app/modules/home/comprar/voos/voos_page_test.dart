import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:app_mobile/app/modules/home/comprar/voos/voos_page.dart';

void main() {
  testWidgets('VoosPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(VoosPage(title: 'Voos')));
    //  final titleFinder = find.text('Voos');
    //  expect(titleFinder, findsOneWidget);
  });
}
