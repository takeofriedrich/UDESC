import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:app_mobile/app/modules/home/comprar/assentos/assentos_page.dart';

void main() {
  testWidgets('AssentosPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AssentosPage(title: 'Assentos')));
    //  final titleFinder = find.text('Assentos');
    //  expect(titleFinder, findsOneWidget);
  });
}
