import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:molhei_o_pe/app/modules/home/mapas/mapas_controller.dart';
import 'package:molhei_o_pe/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // MapasController mapas;
  //
  setUp(() {
    //     mapas = HomeModule.to.get<MapasController>();
  });

  group('MapasController Test', () {
    //   test("First Test", () {
    //     expect(mapas, isInstanceOf<MapasController>());
    //   });

    //   test("Set Value", () {
    //     expect(mapas.value, equals(0));
    //     mapas.increment();
    //     expect(mapas.value, equals(1));
    //   });
  });
}
